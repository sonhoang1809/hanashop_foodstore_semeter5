<nav class="navbar navbar-expand-xl">
    <div class="container h-100">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto h-100">
                <li class="nav-item">
                    @{
                        string active = "";
                        if (ViewBag.SelectedView == "Dashboard")
                        {
                            active = " active";
                        }
                    }
                    @using (Html.BeginForm("Dashboard", "Admin", FormMethod.Post, new { @class = "nav-link" + active }))
                    {
                        <a class="nav-link" href="#0" onclick="$(this).closest('form').submit();">
                            <i class="fas fa-tachometer-alt"></i>
                            <h5>Dashboard</h5>
                        </a>
                    }
                   
                <li class="nav-item">
                    @{         
                        active = "";
                        if (ViewBag.SelectedView == "ManageProduct") {
                            active = " active";
                        }
                    }
                    @using (Html.BeginForm("ManageProduct", "Admin", FormMethod.Get, new { @class = "nav-link" + active })) {
                        <input type="hidden" name="kindID" value="@ViewBag.SelectedKind">
                        <input type="hidden" name="categoryID" value="@ViewBag.SelectedCategory">
                        <a class="nav-link" href="#0" onclick="$(this).closest('form').submit();">
                            <i class="fas fa-shopping-cart"></i>
                            <h5>Foods</h5>                       
                        </a>
                    }
                </li>
                <li class="nav-item">
                    @{ 
                        active = "";
                        if(ViewBag.SelectedView == "ManageUser") {
                            active = " active";
                        }
                    }
                    @using (Html.BeginForm("ManageUser", "Admin", FormMethod.Post, new { @class = "nav-link" + active })) {
                        <a class="nav-link" href="#0" onclick="$(this).closest('form').submit();">
                            <i class="far fa-user"></i>
                            <h5>Accounts</h5>
                        </a>
                    }
                </li>
            </ul>
        </div>
    </div>
</nav>
